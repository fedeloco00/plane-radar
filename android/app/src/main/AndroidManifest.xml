<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- usesCleartextTraffic: the WebView loads the app from our own embedded
         plain-HTTP server at http://127.0.0.1:8765 (see LanWebServer.kt /
         MainActivity.kt) instead of file://, so both this device and other
         devices on the same WiFi run from identical URLs. Android blocks
         cleartext (non-TLS) requests by default from targetSdk 28+, so this
         opt-in is required for that local loopback request to succeed. -->
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.PlaneRadar"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
